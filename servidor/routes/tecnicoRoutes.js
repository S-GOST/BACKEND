import express from 'express'; // Importamos Express para crear el router que manejará las rutas relacionadas con los administradores. Este router se encargará de definir las rutas y asociarlas con los controladores correspondientes para cada operación (obtener, buscar por ID, crear, eliminar, actualizar, login).  
import { obtenerAdmins,obtenerTecPorId, crearTec, eliminarTec, actualizarTec,loginTec } from '../controllers/tecnicoController.js';

const router = express.Router(); // Creamos un router de Express para definir las rutas relacionadas con los administradores. Este router se exportará al final del archivo para ser utilizado en el archivo principal del servidor (server.js) donde se montará en una ruta específica (por ejemplo, '/api/tecni').   

router.get('/obtener', obtenerTec); // Definimos la ruta GET '/obtener' que se asociará con el controlador 'obtenerTecni'. Esta ruta se utilizará para obtener la lista de todos los tecnicos registrados en la base de datos. Cuando un cliente haga una solicitud GET a esta ruta, el controlador 'obtenerAdmins' se encargará de procesar la solicitud, interactuar con el modelo para obtener los datos y devolver la respuesta al cliente.  
router.get('/buscar/:id', obtenerTecniPorId); // Definimos la ruta GET '/buscar/:id' que se asociará con el controlador 'obtenerAdminPorId'. Esta ruta se utilizará para obtener los detalles de un tecnico específico utilizando su ID. El ':id' es un parámetro de ruta que permitirá al cliente especificar el ID del tecnico que desea buscar. Cuando un cliente haga una solicitud GET a esta ruta con un ID específico, el controlador 'obtenerTecniPorId' se encargará de procesar la solicitud, interactuar con el modelo para obtener los datos del tecnico correspondiente y devolver la respuesta al cliente.  
router.post('/login', loginTec); // Definimos la ruta POST '/login' que se asociará con el controlador 'loginTec'. Esta ruta se utilizará para manejar el proceso de inicio de sesión de los tecnicos. Cuando un cliente haga una solicitud POST a esta ruta con las credenciales (usuario y contraseña) en el cuerpo de la solicitud, el controlador 'loginTecni' se encargará de procesar la solicitud, verificar las credenciales contra la base de datos, generar un token JWT si las credenciales son válidas y devolver la respuesta al cliente con el resultado del login.    
router.post('/insertar', crearTecni); // Definimos la ruta POST '/insertar' que se asociará con el controlador 'crearTec'. Esta ruta se utilizará para crear un nuevo tecnico en la base de datos. Cuando un cliente haga una solicitud POST a esta ruta con los datos del nuevo tecnico en el cuerpo de la solicitud, el controlador 'crearTec' se encargará de procesar la solicitud, interactuar con el modelo para insertar el nuevo tecnico en la base de datos y devolver la respuesta al cliente con el resultado de la operación. 
router.put('/actualizar', actualizarTecni);
router.put('/actualizar/:id', actualizarTecni); // Definimos la ruta PUT '/actualizar/:id' que se asociará con el controlador 'actualizarTecni'. Esta ruta se utilizará para actualizar los datos de un tecnico existente en la base de datos utilizando su ID. El ':id' es un parámetro de ruta que permitirá al cliente especificar el ID del tecnico que desea actualizar. Cuando un cliente haga una solicitud PUT a esta ruta con un ID específico y los nuevos datos en el cuerpo de la solicitud, el controlador 'actualizarTecni' se encargará de procesar la solicitud, interactuar con el modelo para actualizar los datos del tecnico correspondiente en la base de datos y devolver la respuesta al cliente con el resultado de la operación.
router.delete('/eliminar/:id', eliminarTecni); // Definimos la ruta DELETE '/eliminar/:id' que se asociará con el controlador 'eliminarTecni'. Esta ruta se utilizará para eliminar un tecnico existente en la base de datos utilizando su ID. El ':id' es un parámetro de ruta que permitirá al cliente especificar el ID del tecnico que desea eliminar. Cuando un cliente haga una solicitud DELETE a esta ruta con un ID específico, el controlador 'eliminarTecni' se encargará de procesar la solicitud, interactuar con el modelo para eliminar el Tecnico correspondiente de la base de datos y devolver la respuesta al cliente con el resultado de la operación.

export default router; // Exportamos el router para que pueda ser utilizado en otras partes de la aplicación, como en el archivo principal del servidor (server.js) donde se montará en una ruta específica (por ejemplo, '/api/tecni').
